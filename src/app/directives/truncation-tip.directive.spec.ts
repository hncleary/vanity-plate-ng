import { TruncationTipDirective } from './truncation-tip.directive';

describe('TruncationTipDirective', () => {
  it('should create an instance', () => {
    const directive = new TruncationTipDirective();
    expect(directive).toBeTruthy();
  });
});
